
* 使い方

Mac環境でC#をコンパイル実行する環境を構築します。
gulp watchでcsファイルを監視して
変更があれば自動的にコンパイルして実行する。

* コマンド

gulp watch // 監視と自動ビルド
gulp build // ビルド

* 準備

node, gulp, monoをインストールする。
コンパイルはMacに初期インストール済みのmcsを使用する。

node, monoをbrewでインストールする
brew install node
brew install mono
